/**
 * Logo - Bear SVG Logo for App Branding
 */

import bear from "@images/bear.svg";

export function Logo({ className }) {
    return <img src={bear} className={className} alt="Randomuser Demo Logo" />;
}
